{% extends "base.html.twig" %}

{% block title %}
    Événements
{% endblock %}

{% block body %}

<div class="container my-4">

       <div class="d-flex justify-content-end mb-3">
            <a  class="btn btn-primary" href="{{ path('app_event_new') }}">Nouveau</a>
        </div>


    <div class="row g-4">
        {% for event in events %}
            <div class="col-12">
                <div class="card h-100">
                    <div class="row g-0">
                        <!-- Date section -->
                        <div class="col-3 bg-light rounded-top rounded-bottom text-center py-2 d-flex flex-column justify-content-center align-items-center">
                            <div class="fw-bold text-uppercase">{{ event.date|date('M') }}</div>
                            <div class="display-6">{{ event.date|date('d') }}</div>
                            <div class="text-muted small">{{ event.date|date('H:i') }}</div>
                        </div>

                        <!-- Event content -->
                        <div class="col-9">
                            <div class="card-body d-flex flex-column h-100">
                                <h5 class="card-title">{{ event.title }}</h5>
                                <div class="mb-2 d-flex justify-content-between flex-wrap small text-muted">
                                    <span>Le {{ event.date|date('d/m/Y') }}</span>
                                    <span class="badge text- bg-dark bg-gradient">{{ event.nbrOfPlaces }} places</span>
                                </div>
                                <p class="card-text flex-grow-1">{{ event.description }}</p>

                                <div class="d-flex justify-content-between align-items-center mt-2">
                                    <div>
                                        <span class="me-1">📍</span>{{ event.location }}
                                    </div>
                                    <div class="d-flex gap-2">
        								{% if event.participants.contains(app.user) %}
                                        <a class="btn btn-sm btn-secondary" href="{{ path('app_event_join', {'id': event.id}) }}">Se désinscrire</a>
		            					{% else %}
					        			<a class="btn btn-sm btn-primary" href="{{ path('app_event_join', {'id': event.id}) }}">S'inscrire</a>
							            {% endif %}
							            <a class="btn btn-sm btn-info" href="{{path('app_event_show', {id: event.id})}}">Détails</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div> <!-- row g-0 -->
                </div> <!-- card -->
            </div> <!-- col -->
        {% endfor %}
    </div> <!-- row -->
</div> <!-- container -->
{% endblock %}
